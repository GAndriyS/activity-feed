import { createTheme } from "@mui/material/styles";

const palette = {
  background: {
    default: "#fff",
  },
  primary: {
    main: "#00c6ee",
  },
  success: {
    main: "#52c8b4",
  }
}

export default createTheme({
  palette
});